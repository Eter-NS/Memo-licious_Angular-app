import { AdaptiveButtonDirective } from './adaptive-button.directive';

describe('AdaptiveButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new AdaptiveButtonDirective();
    expect(directive).toBeTruthy();
  });
});
