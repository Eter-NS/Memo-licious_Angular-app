import { SvgElementsDirective } from './svg-elements.directive';

describe('SvgElementsDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgElementsDirective();
    expect(directive).toBeTruthy();
  });
});
