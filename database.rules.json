{
  "rules": {
    "users" :{
      "$uid" : {
        ".read" : "$uid === auth.uid", // if the uID equals the ID from the request, allow to read and write to the path.
        ".write" : "$uid === auth.uid"
      }
    }
  }
}